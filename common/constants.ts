export const APP_ID = require('../package.json').name

export const APP_NAME = 'Trbox Dashboard'

export const APP_TITLE = 'TRBOX'

export const VERSION = require('../package.json').version

export const PORT_WS = 8000
